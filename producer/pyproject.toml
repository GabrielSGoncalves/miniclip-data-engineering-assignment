[project]
name = "producer"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "confluent-kafka==2.3.0",
    "faker==22.0.0",
    "jsonschema==4.21.1",
]

[dependency-groups]
dev = [
    "pytest>=7.4.4",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
